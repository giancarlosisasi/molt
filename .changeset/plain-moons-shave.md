---
"molt-release": patch
---

The banner glyph is a snake. It was the butterfly changesets uses, which molt inherited along with
the rest of the port; the tool is a Python one and now carries its own mark. Nothing else about the
banner changed, and the console still degrades the glyph to plain text on a legacy code page.
